package service

/*
import (
	"time"

	"github.com/PKL-Angkasa-Pura-I/backend-pkl/helper"
)

func (s *svc) CountQuotaService(start, end time.Time, id_divisi int) error {
	_, count_month, _, _, _, _ := helper.Diff(start, end)

	division,err := s.repo.GetDivisionByID(id_divisi)
	if err != nil {
		return err
	}

	for i := 1; i <= count_month; i++ {
		start_range := start
		end_range := start.AddDate(0, 1, 0)
	}

	return nil
}
*/
